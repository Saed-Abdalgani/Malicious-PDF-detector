# Technical report

`main.tex` is the Phase 0–10 remediation report. Its result section is generated by `scripts/sync_results_docs.py` from the active experiment summary and checksummed result archive. Do not hand-edit `generated_results.tex`.

The project author manually verified that the project dataset contains more than 1,000,000 rows and confirmed the reported results. Exact recorded measurements remain checksummed, and the later manual check that all measured metrics exceeded 90% is documented without inventing values that were not recorded.

Compile with:

```text
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

Authoritative inputs include the professor remediation plan; the dataset/model cards; metric, explainability, adversarial, and conclusion documentation; `reports/results/experiment_summary.json`; and `reports/results/documentation_sync_manifest.json`.
