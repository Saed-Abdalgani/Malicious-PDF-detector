# Technical report status

`main.tex` is the Phase 0–10 remediation report. Its result section is generated by `scripts/sync_results_docs.py` from the active experiment summary and checksummed historical archive. Do not hand-edit `generated_results.tex`.

The current active experiment remains at Phase 0 because no enabled, approved primary feature table satisfies the 2.65M-input/2.5M-clean/2M-train and 99.5%-benign gates. Therefore the report makes no verified final model-performance claim. It separately preserves exact pre-remediation manual measurements as historical/unverified and records the author's later “all above 90%” manual re-check as author-reported without inventing exact values.

Compile with:

```text
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

Authoritative inputs include the professor remediation plan; the dataset/model cards; metric, explainability, adversarial, and conclusion documentation; `reports/results/experiment_summary.json`; and `reports/results/documentation_sync_manifest.json`.
